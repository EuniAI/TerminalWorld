# Docker Compose Best Practices (For Review & Optimization)

This document provides rules for Step 2 (Generation Phase). 
*Note: Use these guidelines to review, enhance, or fix the `docker-compose.yaml` generated by the script. Ensure the generated services have proper healthchecks, volumes, and networks.*

## Key Compose Principles

- **Main service MUST be named `app`**: The evaluation harness connects to the service named `app`. Any other name (e.g. `web`, `server`) will cause the harness to fail to attach. Always use `app` as the primary service name.
- Use `depends_on` with `condition: service_healthy` for startup ordering.
- Use named volumes for data persistence.
- Use internal networks for service isolation (`networks: backend`).
- **Service name = hostname**: containers reach each other by service name, not IP (e.g., `db:5432`, `redis:6379`).
- **Never expose database ports to the host unless explicitly requested**: Use internal networks. The app container will reach the DB via the internal network. Only map ports (e.g., `ports: ["5432:5432"]`) if the user specifically needs external access.

## Common Connection Strings

```
# PostgreSQL
DATABASE_URL=postgresql://user:password@postgres:5432/app
# MySQL
DATABASE_URL=mysql://user:password@mysql:3306/app
# MongoDB
MONGO_URI=mongodb://admin:adminpass@mongodb:27017/app?authSource=admin
# Redis
REDIS_URL=redis://redis:6379
```

## Optional Debug Services (Profiles)

Use `profiles: [debug]` to include GUI/admin tools that should not start by default, but are available for debugging.

```yaml
  adminer:
    image: adminer
    profiles: [debug]
    ports:
      - "8080:8080"
    networks:
      - backend

  pgadmin:
    image: dpage/pgadmin4
    profiles: [debug]
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    networks:
      - backend
```